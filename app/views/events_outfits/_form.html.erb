<%= form_for(@events_outfit) do |f| %>
  <% if @events_outfit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@events_outfit.errors.count, "error") %> prohibited this events_outfit from being saved:</h2>

      <ul>
      <% @events_outfit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.collection_select :event_id, Event.all, :id, :title %>
  </div>
  <div class="field">
    <%= f.label :outfit_id %><br />
    <%= f.collection_select :outfit_id, Outfit.all, :id, :id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
