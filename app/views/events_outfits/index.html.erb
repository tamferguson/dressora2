<h1>Listing events_outfits</h1>

<table>
  <tr>
    <th>Event</th>
    <th>Outfit</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @events_outfits.each do |events_outfit| %>
  <tr>
    <td><%= events_outfit.event_id %></td>
    <td><%= events_outfit.outfit_id %></td>
    <td><%= link_to 'Show', events_outfit %></td>
    <td><%= link_to 'Edit', edit_events_outfit_path(events_outfit) %></td>
    <td><%= link_to 'Destroy', events_outfit, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Events outfit', new_events_outfit_path %>
